#include <stdio.h>

/* Escrever um algoritmo que recebe 15 números reais do usuário, determina e escreve a soma dos valores positivos e a
quantidade de valores negativos lidos */
/* int main(){
int num, soma = 0, cont = 0, qtd = 0;

do
{printf("Informe um  numero: ");
 scanf("%d", &num);
    if (num >= 0){
        soma = soma + num; 
    }
    else {
        qtd++;
    }
    cont++; 

} while (cont < 15);
    printf("A soma dos valores Positivos eh: %d\n", soma);
    printf("A quantidade de valores negativos eh:%d", qtd);
    return 0;
}  */

/* Escreva um programa que receba um número inteiro do  usuário, calcule e escreva a tabuada deste número utilizando 
uma estrutura de repetição.
• Ex.: Digite um número: 2 
2x1=2
2x2=4
2x3=6.. até 10. */

/*  int main(){
 int num, cont = 1;
 printf("DIGITE UM NUMERO PARA A SUA TABUADA: ");
 scanf("%d", &num);
 do{
    printf("%d * %d = %d\n", num, cont, (num * cont));
    cont++;

 } while(cont <= 10);

    return 0;
 } */


